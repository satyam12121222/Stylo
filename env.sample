# Copy to .env for local dev (do NOT commit .env)
# Set these in Render → Environment for production

# Server
PORT=10000
NODE_ENV=production

# Database
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/stylo?retryWrites=true&w=majority&appName=Cluster0
MONGODB_URI=

# Auth
JWT_SECRET=

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Stripe (optional)
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
